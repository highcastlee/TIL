
# 딕셔너리 (Dictionary)
  - key-value 형태로 다양한 자료형 개체(entity)를 저장하는 자료구조 
  - Map과 유사하다.



### 딕셔너리 구현
```javascript
function Dictionary(items = {}) {
  this.items = items;
}

Dictionary.prototype.getBuffer = function () {
  // 객체도 spread operator 사용 가능
  return { ...this.items };
};

Dictionary.prototype.clear = function () {
  this.items = {};
};

Dictionary.prototype.size = function () {
  // Object.keys(this.items)는 key 값으로 이루어진 배열 반환
  return Object.keys(this.items).length;
};

// has(): 개체 존재 여부 확인. 키 존재 유무에 따라 Boolean 값 반환
Dictionary.prototype.has = function (key) {
  return this.items.hasOwnProperty(key);
};

// set(key, value): 개체 추가
Dictionary.prototype.set = function (key, value) {
  this.items[key] = value;
};

// get(key): 특정 key를 갖는 개체의 value 반환. key 없으면 undefined
Dictionary.prototype.get = function (key) {
  return this.has(key) ? this.items[key] : undefined;
};

// remove(key): 해당 key를 가진 개체 삭제
Dictionary.prototype.remove = function (key) {
  if (this.has(key)) {
    delete this.items[key];
    return true;
  }
  return false;
};

let dict = new Dictionary();
dict.set("age", 20);

```

